{
  "compilerOptions": {
      "strict": true,
      "module": "commonjs", // 컴파일된 js 코드는 물리적으로 동작하는 방식이 다른 (웹(amd), Node(commonjs)) 동작해야함.  
      "moduleResolution": "node",
      "lib": ["es2015","es2016","es2017","es2018","es2019","es2020"], // 허용할 js 버전
      "target": "ES5", // 트랜스파일할 대상 js의 버전을 설정. ts 코드가 해당 버전으로 트랜스파일
      "outDir": "./dist", 
      "esModuleInterop": true // export default가 없는 라이브러리도 * as 사용 없이 import 허용.
  },
  "exclude": ["node_modules"],
  "include": ["src/**/*"]
}